name: 'Release - Stencil Storybook'

on:
  workflow_dispatch:
    inputs:
      channel:
        description: 'Which release workflow should run?'
        required: true
        type: choice
        default: dev
        options:
          - dev
          - production
      bump:
        description: 'Semver bump to apply (patch, minor, major). For dev releases this only affects the computed prerelease version, not the committed package.json.'
        required: false
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major

permissions:
  contents: write
  id-token: write

jobs:
  run-dev:
    if: ${{ inputs.channel == 'dev' }}
    uses: ./.github/workflows/release-dev.yml
    secrets: inherit
    with:
      bump: ${{ inputs.bump }}

  run-production:
    if: ${{ inputs.channel == 'production' }}
    uses: ./.github/workflows/release-production.yml
    secrets: inherit
    with:
      bump: ${{ inputs.bump }}
